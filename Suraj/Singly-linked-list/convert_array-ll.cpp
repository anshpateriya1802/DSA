#include <bits/stdc++.h>
using namespace std;
class Node {
public:
    public:
    int data;
    Node* next;
    Node(int data) {
        this->data = data;
        this->next = NULL;
    }
    Node(int data , Node* next){
        this->data = data;
        this->next = next;
    }
};

int main() {
    vector<int> arr = {5, 2, 9, 1, 5, 6};
    
    
    return 0;
}